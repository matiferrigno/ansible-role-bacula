###############################
## Auto-generated by:        ##
##         Ansible           ##
## Don't do changes by hand  ##
## Role: matiferrigno.bacula ##
###############################

Pool {
    Name = {{ item.name }}-Full-Pool
    Pool Type = Backup
    Recycle = yes
    AutoPrune = yes
    Volume Retention = 15day
    Maximum Volume Jobs = 3
    Use Volume Once = yes
    Cleaning Prefix = Clean
    LabelFormat = "{{ item.name }}-Full-"
}

Pool {
    Name = {{ item.name }}-Diff-Pool
    Pool Type = Backup
    Recycle = yes
    AutoPrune = yes
    Volume Retention = 15day
    Maximum Volume Jobs = 10
    Use Volume Once = yes
    Cleaning Prefix = Clean
    LabelFormat = "{{ item.name }}-Diff-"
}

Pool {
    Name = {{ item.name }}-Inc-Pool
    Pool Type = Backup
    Recycle = yes
    AutoPrune = yes
    Volume Retention = 15day
    Maximum Volume Jobs = 10
    Use Volume Once = yes
    Cleaning Prefix = Clean
    LabelFormat = "{{ item.name }}-Inc-"
}